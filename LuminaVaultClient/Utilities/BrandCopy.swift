import Foundation

/// Single source of truth for brand taglines and positioning copy.
/// Marketing repositioning = edit here, not in individual views.
enum BrandCopy {
    /// Primary motto shown under the app name.
    static let motto = "Infra brain for AI agents."

    /// Dashboard status line once the vault is reachable.
    static let brainOnline = "Your infra brain is online."

    /// Vault creation hero line.
    static let createVault = "Let's build your agent's brain."

    /// SOUL quiz confirmation title.
    static let soulConfirmTitle = "Your agent, in your voice"
}
